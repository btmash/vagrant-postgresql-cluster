local   all         all                               trust
host    all         all         127.0.0.1/32          trust
{% for host in pgpool_allowed_hba %}
host  {{ host.database }} {{ host.name }} {{ host.host }}/32 md5
{% endfor %}
