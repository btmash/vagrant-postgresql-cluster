input {
  lumberjack {
    port => 5000
    type => "logs"
    ssl_certificate => "{{ logstash_server_ssl_cert_dir }}/logstash-forwarder.crt"
    ssl_key => "{{ logstash_server_private_key_dir }}/logstash-forwarder.key"
  }
}
