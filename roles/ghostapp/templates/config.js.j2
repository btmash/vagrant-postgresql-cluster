// # Ghost Configuration
// Setup your Ghost install for various environments
// Documentation can be found at http://support.ghost.org/config/

var path = require('path'),
    config;

config = {
    // ### Production
    // When running Ghost in the wild, use the production environment
    // Configure your URL and mail settings here
    production: {
        url: '{{ ghostapp_site_url }}',
        mail: {},
        database: {
            client: '{{ ghostapp_db.prod.type }}',
            connection: {
                host     : '{{ ghostapp_db.prod.host }}',
                user     : '{{ ghostapp_db.prod.user }}',
                password : '{{ ghostapp_db.prod.pass }}',
                database : '{{ ghostapp_db.prod.database }}',
                charset  : '{{ ghostapp_db.prod.charset }}'
            },
            debug: false
        },

        server: {
            // Host to be passed to node's `net.Server#listen()`
            host: '0.0.0.0',
            // Port to be passed to node's `net.Server#listen()`, for iisnode set this to `process.env.PORT`
            port: '2368'
        }
    },

    // ### Development **(default)**
    development: {
        // The url to use when providing links to the site, E.g. in RSS and email.
        // Change this to your Ghost blogs published URL.
        url: '{{ ghostapp_site_url }}',

        // Example mail config
        // Visit http://support.ghost.org/mail for instructions
        // ```
        //  mail: {
        //      transport: 'SMTP',
        //      options: {
        //          service: 'Mailgun',
        //          auth: {
        //              user: '', // mailgun username
        //              pass: ''  // mailgun password
        //          }
        //      }
        //  },
        // ```

        database: {
            client: '{{ ghostapp_db.dev.type }}',
            connection: {
                host     : '{{ ghostapp_db.dev.host }}',
                user     : '{{ ghostapp_db.dev.user }}',
                password : '{{ ghostapp_db.dev.pass }}',
                database : '{{ ghostapp_db.dev.database }}',
                charset  : '{{ ghostapp_db.dev.charset }}'
            },
            debug: false
        },
        server: {
            // Host to be passed to node's `net.Server#listen()`
            host: '0.0.0.0',
            // Port to be passed to node's `net.Server#listen()`, for iisnode set this to `process.env.PORT`
            port: '2368'
        },
        paths: {
            contentPath: path.join(__dirname, '/content/')
        }
    },
};

// Export config
module.exports = config;
